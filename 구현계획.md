Phase 1: 프로젝트 기반 설정
PR 제목: feat: 프로젝트 기반 설정 - 타입, 샘플 데이터, 유틸
목적:

코드 작성의 기반이 되는 타입과 데이터 준비
UI 없이 데이터 구조만 먼저 확정

포함 커밋:

Commit 1-4: 타입 정의 (Event, Track, Timeline 설정)
Commit 5-7: 샘플 데이터 (트랙 2개, 이벤트 7개)
Commit 8-9: 날짜 유틸 함수

완료 기준:

TypeScript 컴파일 에러 없음
샘플 데이터가 설계서와 일치
날짜 변환 함수 단위 테스트 가능

예상 시간: 1.5시간
리뷰 포인트:

타입 정의가 확장 가능한가?
ID 네이밍 일관성 (evt1, t1)
유틸 함수 엣지 케이스 처리

Phase 2: 기본 UI 레이아웃
PR 제목: feat: 기본 UI 레이아웃 - 헤더, 빈 타임라인 뷰
목적:

앱의 기본 레이아웃 구조 잡기
줌/팬 라이브러리 설치 및 기본 설정

포함 커밋:

Commit 10: react-zoom-pan-pinch 설치
Commit 11: 빈 TimelineView 컴포넌트
Commit 12: TransformWrapper 기본 설정
Commit 13: 타임라인 컨테이너 기본 구조

완료 기준:

헤더와 메인 영역이 화면을 꽉 채움
마우스 휠로 줌 작동
마우스 드래그로 팬 작동
3000px 너비 타임라인 컨테이너 표시

예상 시간: 1시간
리뷰 포인트:

TransformWrapper 설정값 적절한가?
레이아웃이 반응형으로 작동하는가?

Phase 3: 트랙 시스템
PR 제목: feat: 트랙 렌더링 - 2개 트랙 표시
목적:

타임라인의 수평 레이어(트랙) 구현
트랙별 배경색과 이름 표시

포함 커밋:

Commit 14: 빈 TrackRow 컴포넌트 생성
Commit 15: TrackRow에 트랙 이름 추가
Commit 16: TimelineView에 트랙 렌더링

완료 기준:

2개 트랙이 수직으로 쌓임
각 트랙 배경색 구분 (회색, 노란색)
트랙 이름 왼쪽 상단에 표시
줌/팬 시 트랙도 함께 이동

예상 시간: 1시간
리뷰 포인트:

트랙 높이가 적절한가?
트랙 추가 시 확장 가능한 구조인가?

Phase 4: 시간 축 (월 눈금)
PR 제목: feat: 월별 눈금 렌더링 - 2025년 12개월 표시
목적:

시간의 흐름을 나타내는 눈금 구현
사용자가 현재 보고 있는 시기 파악 가능

포함 커밋:

Commit 17: MonthTicks 컴포넌트 생성 (빈 구조)
Commit 18: MonthTicks 눈금선 렌더링
Commit 19: MonthTicks 월 레이블 추가
Commit 20: TimelineView에 MonthTicks 추가

완료 기준:

12개 수직 눈금선 균등 배치
각 눈금에 "1월", "2월" 등 레이블
눈금이 트랙 위에 오버레이
줌 인 시 눈금 간격 확대

예상 시간: 1시간
리뷰 포인트:

눈금 위치 계산이 정확한가?
레이블 가독성은 충분한가?

Phase 5: 이벤트 렌더링
PR 제목: feat: 이벤트 박스 렌더링 - 7개 샘플 이벤트 표시
목적:

타임라인의 핵심인 이벤트 시각화
시간의 밀도를 실제로 볼 수 있게 함

포함 커밋:

Commit 21: EventBox 컴포넌트 생성 (빈 구조)
Commit 22: EventBox 위치 및 크기 계산
Commit 23: EventBox 내용 및 스타일링
Commit 24: TrackRow에 이벤트 props 추가
Commit 25: TrackRow에서 EventBox 렌더링
Commit 26: TimelineView에서 이벤트 데이터 전달

완료 기준:

7개 이벤트 모두 표시
이벤트 위치가 날짜와 정확히 일치
이벤트 너비가 기간에 비례
호버 시 툴팁으로 설명 표시
1일 이벤트는 고정 너비(60px)

예상 시간: 1.5시간
리뷰 포인트:

이벤트 겹침 시각적으로 확인 가능한가?
짧은 이벤트도 클릭 가능한 크기인가?
색상 대비가 충분한가?

Phase 6: 코드 정리 및 문서화
PR 제목: docs: Phase 1 완료 - 문서화 및 코드 정리
목적:

코드 가독성 향상
다음 개발자를 위한 문서 작성

포함 커밋:

Commit 27: 컴포넌트 export 정리
Commit 28: README 작성 및 Phase 1 완료

완료 기준:

모든 컴포넌트가 index.ts로 export
README에 실행 방법, 사용법 명시
README에 완료된 기능 체크리스트
TypeScript 타입 체크 통과
ESLint 경고 0개

예상 시간: 30분
리뷰 포인트:

README가 명확한가?
import 경로가 일관적인가?

Phase 7: 백엔드 API 기본 구조
PR 제목: feat: 백엔드 API - Express 서버 및 기본 라우팅
목적:

이벤트 데이터를 영구 저장할 백엔드 준비
JSON 파일 기반 간단한 API

포함 작업:

Express 서버 설정
backend/src/data/events.json 생성
backend/src/data/tracks.json 생성
기본 라우팅 (GET /api/health)
CORS 설정

완료 기준:

http://localhost:3001/api/health 응답
JSON 파일에 샘플 데이터 저장
프론트엔드에서 백엔드 연결 가능

예상 시간: 1시간
리뷰 포인트:

에러 처리가 적절한가?
JSON 파일 읽기/쓰기 동기화 이슈는?

Phase 8: 이벤트 조회 API
PR 제목: feat: 이벤트 조회 API - GET /api/events, /api/tracks
목적:

프론트엔드가 실제 데이터를 서버에서 가져오게

포함 작업:

GET /api/events 엔드포인트
GET /api/tracks 엔드포인트
프론트엔드 API 클라이언트 (src/services/api.ts)
TanStack Query 설정 (useEvents, useTracks 훅)

완료 기준:

프론트엔드가 서버에서 이벤트 조회
로딩 상태 표시
에러 상태 처리

예상 시간: 1.5시간
리뷰 포인트:

API 응답 타입이 프론트엔드 타입과 일치하는가?
TanStack Query 캐싱 설정 적절한가?

Phase 9: 이벤트 생성 UI
PR 제목: feat: 이벤트 생성 모달 - UI 컴포넌트
목적:

사용자가 새 이벤트를 추가할 수 있는 UI

포함 작업:

src/components/ui/Modal.tsx 생성
src/components/EventModal/EventCreateModal.tsx 생성
src/components/EventModal/EventForm.tsx 생성
"새 이벤트" 버튼 추가

완료 기준:

"새 이벤트" 버튼 클릭 시 모달 열림
폼 필드: 제목, 트랙, 시작일, 종료일, 설명
취소 버튼으로 모달 닫힘
아직 저장은 안 됨 (다음 Phase)

예상 시간: 1.5시간
리뷰 포인트:

폼 레이아웃이 직관적인가?
모달 접근성(a11y) 고려되었는가?

Phase 10: 이벤트 생성 API 연동
PR 제목: feat: 이벤트 생성 API - POST /api/events
목적:

새 이벤트를 서버에 저장

포함 작업:

POST /api/events 엔드포인트
프론트엔드 useCreateEvent 훅 (TanStack Query mutation)
폼 검증 로직 (필수 항목, 날짜 유효성)
저장 성공 시 모달 닫기

완료 기준:

폼 제출 시 백엔드로 POST 요청
events.json 파일에 새 이벤트 추가
타임라인에 즉시 반영 (TanStack Query 캐시 갱신)
필수 항목 미입력 시 에러 메시지

예상 시간: 1.5시간
리뷰 포인트:

ID 생성 로직 (UUID vs 순차)
에러 처리 완전한가?

Phase 11: 이벤트 삭제 기능
PR 제목: feat: 이벤트 삭제 - DELETE /api/events/:id
목적:

불필요한 이벤트 제거 가능

포함 작업:

DELETE /api/events/:id 엔드포인트
EventBox에 삭제 버튼 추가 (호버 시 표시)
삭제 확인 다이얼로그
useDeleteEvent 훅

완료 기준:

이벤트 호버 시 삭제 버튼 표시
클릭 시 "정말 삭제하시겠습니까?" 확인
삭제 시 타임라인에서 즉시 제거
events.json에서 삭제됨

예상 시간: 1시간
리뷰 포인트:

실수로 삭제 방지 장치 충분한가?
삭제 버튼 UX가 직관적인가?

Phase 12: 토스트 알림 시스템
PR 제목: feat: 토스트 알림 - 성공/에러 피드백
목적:

사용자 액션에 대한 시각적 피드백

포함 작업:

src/components/ui/Toast.tsx 생성
토스트 컨텍스트 또는 전역 상태
생성 성공: "이벤트가 생성되었습니다"
삭제 성공: "이벤트가 삭제되었습니다"
에러 발생: "오류가 발생했습니다"

완료 기준:

이벤트 생성 성공 시 토스트 표시
이벤트 삭제 성공 시 토스트 표시
API 에러 시 토스트 표시
3초 후 자동 사라짐

예상 시간: 1시간
리뷰 포인트:

여러 토스트 동시 표시 시 쌓임 처리
토스트 위치 적절한가?

Phase 13: 로딩 및 에러 상태 개선
PR 제목: feat: UX 개선 - 로딩 상태, 에러 처리
목적:

네트워크 지연 시 사용자 경험 개선

포함 작업:

타임라인 로딩 스켈레톤
이벤트 생성 중 버튼 비활성화
에러 발생 시 재시도 버튼
빈 상태 처리 (이벤트 0개일 때)

완료 기준:

초기 로딩 시 스켈레톤 표시
생성/삭제 중 버튼 "로딩..." 표시
API 에러 시 "다시 시도" 버튼
이벤트 없을 때 안내 메시지

예상 시간: 1.5시간
리뷰 포인트:

로딩 상태가 자연스러운가?
에러 메시지가 사용자 친화적인가?

Phase 14: 최종 테스트 및 문서화
PR 제목: docs: 프로젝트 완료 - 최종 문서화
목적:

데모 완성도 검증
전체 프로젝트 문서 정리

포함 작업:

README 전체 업데이트
스크린샷 추가
사용 시나리오 작성
알려진 제한사항 명시
향후 개선 계획

완료 기준:

README에 전체 기능 설명
설치 및 실행 가이드 명확
데모 영상 또는 GIF
모든 Phase 체크리스트 완료

예상 시간: 1시간
리뷰 포인트:

문서가 신규 개발자도 이해 가능한가?
데모가 핵심 가치("시간의 밀도")를 보여주는가?
